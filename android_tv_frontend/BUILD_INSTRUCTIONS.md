# Android TV Frontend — Build Instructions

Project root: android_tv_frontend/

Build (Debug):
- ./gradlew assembleDebug

APK Output:
- app/build/outputs/apk/debug/app-debug.apk

Alternative from repository root:
- ./gradlew :app:assembleDebug   (uses includeBuild forwarding)
