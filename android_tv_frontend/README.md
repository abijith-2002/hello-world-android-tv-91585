# Android TV Frontend (Gradle Root)

This directory is the root of the Android/Kotlin Gradle project.

Key indicators:
- `settings.gradle` / `settings.gradle.kts`
- `build.gradle` / `build.gradle.kts`
- `gradlew`, `gradlew.bat`
- Modules: `app`, `list`, `utilities`

If a tool needs the project root, point it here: `hello-world-android-tv-91585/android_tv_frontend`.
