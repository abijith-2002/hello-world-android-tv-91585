# Gradle Forwarder Tasks

From repository root, you can run:
- ./gradlew -I root-forwarder.gradle.kts forwardAssembleDebug

This invokes the nested Gradle wrapper inside android_tv_frontend to build the debug APK.
