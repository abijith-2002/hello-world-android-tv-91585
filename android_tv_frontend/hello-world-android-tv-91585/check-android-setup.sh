#!/usr/bin/env bash
set -e
echo "Android Gradle project is nested under: android_tv_frontend"
echo "Forwarding via settings.gradle includeBuild from repository root is configured."
echo "Use: ./gradlew :app:assembleDebug  OR  cd android_tv_frontend && ./gradlew assembleDebug"
exit 0
