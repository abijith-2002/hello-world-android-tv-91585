# Android Project Root

The Android TV project lives under:

- android_tv_frontend/

Build commands (from repo root):

- ./gradlew :app:assembleDebug
- Or run helper: ./run-android-build.sh

These commands delegate to the nested Gradle wrapper under `android_tv_frontend/`.
