This repository contains an Android/Kotlin Gradle project.

Project root:
- This directory (contains `settings.gradle`, `gradlew`, `gradle/`)

Primary application module:
- app (module path: `:app`)

Additional modules:
- list (module path: `:list`)
- utilities (module path: `:utilities`)

Build command:
- ./gradlew assembleDebug

APK output:
- app/build/outputs/apk/debug/app-debug.apk
