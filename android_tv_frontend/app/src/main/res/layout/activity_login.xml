<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/loginRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/nord_bg">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingStart="64dp"
        android:paddingEnd="64dp"
        android:paddingTop="48dp"
        android:paddingBottom="48dp">

        <com.google.android.material.card.MaterialCardView
            android:id="@+id/loginCard"
            style="@style/Widget.Card.Nord"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:foreground="@drawable/tv_focus_ripple"
            app:layout_constraintWidth_min="360dp"
            app:layout_constraintWidth_max="720dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="24dp">

                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal"
                    android:gravity="center_vertical">

                    <ImageButton
                        android:id="@+id/btnBack"
                        android:layout_width="48dp"
                        android:layout_height="48dp"
                        android:background="@drawable/card_bg"
                        android:contentDescription="@string/back"
                        android:focusable="true"
                        android:padding="8dp"
                        android:src="@drawable/ic_launcher_foreground" />

                    <TextView
                        android:id="@+id/loginTitle"
                        style="@style/TextAppearance.Card.Title"
                        android:layout_width="0dp"
                        android:layout_height="wrap_content"
                        android:layout_marginStart="12dp"
                        android:text="@string/login_title"
                        android:textStyle="bold"
                        android:layout_weight="1"/>
                </LinearLayout>

                <Space
                    android:layout_width="1dp"
                    android:layout_height="16dp" />

                <TextView
                    android:id="@+id/usernameLabel"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="@string/label_username_email"
                    android:textColor="@color/nord_on_surface"
                    android:textSize="18sp"
                    android:paddingStart="4dp"
                    android:paddingBottom="8dp" />

                <EditText
                    android:id="@+id/inputUsername"
                    android:layout_width="match_parent"
                    android:layout_height="56dp"
                    android:background="@drawable/card_bg"
                    android:paddingStart="16dp"
                    android:paddingEnd="16dp"
                    android:textColor="@color/nord_on_surface"
                    android:textColorHint="@color/nord_grey_500"
                    android:hint="@string/hint_username_email"
                    android:singleLine="true"
                    android:focusable="true"
                    android:nextFocusDown="@id/inputPassword"
                    android:nextFocusUp="@id/btnBack" />

                <Space
                    android:layout_width="1dp"
                    android:layout_height="16dp" />

                <TextView
                    android:id="@+id/passwordLabel"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="@string/label_password"
                    android:textColor="@color/nord_on_surface"
                    android:textSize="18sp"
                    android:paddingStart="4dp"
                    android:paddingBottom="8dp" />

                <EditText
                    android:id="@+id/inputPassword"
                    android:layout_width="match_parent"
                    android:layout_height="56dp"
                    android:background="@drawable/card_bg"
                    android:paddingStart="16dp"
                    android:paddingEnd="16dp"
                    android:textColor="@color/nord_on_surface"
                    android:textColorHint="@color/nord_grey_500"
                    android:hint="@string/hint_password"
                    android:singleLine="true"
                    android:focusable="true"
                    android:nextFocusDown="@id/btnSubmit"
                    android:nextFocusUp="@id/inputUsername" />

                <Space
                    android:layout_width="1dp"
                    android:layout_height="12dp" />

                <TextView
                    android:id="@+id/errorText"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:textColor="@color/nord_grey_300"
                    android:visibility="gone"
                    android:textSize="14sp"
                    android:paddingStart="4dp" />

                <Space
                    android:layout_width="1dp"
                    android:layout_height="20dp" />

                <Button
                    android:id="@+id/btnSubmit"
                    android:layout_width="match_parent"
                    android:layout_height="64dp"
                    android:text="@string/action_sign_in"
                    android:backgroundTint="@color/nord_grey_400"
                    android:textColor="@color/nord_bg"
                    android:focusable="true"
                    android:foreground="@drawable/tv_focus_ripple"
                    android:nextFocusUp="@id/inputPassword"
                    android:nextFocusLeft="@id/btnSubmit"
                    android:nextFocusRight="@id/btnSubmit" />

            </LinearLayout>

        </com.google.android.material.card.MaterialCardView>

    </androidx.constraintlayout.widget.ConstraintLayout>
</ScrollView>
