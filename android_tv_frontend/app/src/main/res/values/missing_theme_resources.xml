<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- PUBLIC_INTERFACE -->
    <!-- Keep only unique fallback resources that do not exist in other values files to avoid duplicates. -->
    <!-- Dimensions -->
    <dimen name="card_corner_radius">12dp</dimen>

    <!-- Do not redefine colors that already exist in colors.xml -->
    <!-- If additional unique placeholders are needed in the future, add them here ensuring no duplication. -->
</resources>
