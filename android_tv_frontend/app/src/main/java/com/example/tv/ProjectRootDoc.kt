package com.example.tv

/**
 * PUBLIC_INTERFACE
 * ProjectRootDoc
 * This file exists as a harmless anchor within the :app module's main source set.
 * Some static analyzers require at least one top-level Kotlin file present to
 * reliably identify the module and project root without relying on meta files.
 *
 * It does not change runtime behavior and contains no logic.
 */
object ProjectRootDoc
