# Android TV Frontend - Build Entry

This module is the Gradle root for the Android TV app.

- Root Gradle settings: `settings.gradle`
- Wrapper: `./gradlew`
- Modules: `:app`, `:list`, `:utilities`

Build examples (run from this folder):
- `./gradlew :app:assembleDebug`
- `./gradlew clean`

If any tooling requires an explicit project root, use this directory:
`hello-world-android-tv-91585/android_tv_frontend`.

This file is purely informational and does not affect the build.
