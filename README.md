# Hello World Android TV

- DPAD_UP from the first content rail moves focus to the top menu (menuHome). Implemented in HomeActivity.
- Build: ./gradlew assembleDebug (wrapper delegates to android_tv_frontend/gradle wrapper)
